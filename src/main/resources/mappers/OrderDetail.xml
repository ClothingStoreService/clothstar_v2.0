<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.store.clothstar.orderDetail.repository.MybatisOrderDetailRepository">

    <insert id="saveOrderDetail" parameterType="org.store.clothstar.orderDetail.domain.OrderDetail"
            useGeneratedKeys="true" keyProperty="orderDetailId" keyColumn="order_detail_id">
        INSERT INTO order_detail( order_detail_id, product_line_id, order_id, product_id, quantity, fixed_price,
        onekind_total_price, name, stock, option_name, brand_name
        )
        VALUES( #{orderDetailId}, #{productLineId}, #{orderId}, #{productId}, #{quantity}, #{fixedPrice},
        #{oneKindTotalPrice}, #{name}, #{stock}, #{optionName}, #{brandName} )
    </insert>

    <select id="findByOrderId" resultType="org.store.clothstar.orderDetail.domain.OrderDetail">
        select * from order_detail where order_id = #{orderId}
    </select>

</mapper>